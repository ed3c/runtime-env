# `tests/`

Owner: public-seam, regression, hollow, mutation, schema, consumer-sync, workload, credential-broker, carrier-policy, and repository-safety controls.

`bash tests/run-all.sh` is the aggregate local suite. Fixtures prove deterministic behavior; they do not proxy a real provider/account/browser/device/session. A skipped optional provider remains named separately from PASS.
