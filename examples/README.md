# `examples/`

Owner: deterministic, secret-free projections generated from current catalog/module/profile logic.

```text
producer output → checked-in example → byte comparison in tests
```

Do not hand-edit examples as source truth. Change the owning contract/producer and regenerate. Placeholder names are not credential values or live readiness evidence.
