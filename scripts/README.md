# `scripts/`

Owner: bounded helpers for provider bootstrap, consumer CLI installation, JDK verification, and local-runtime verification.

Scripts must use fixed operations, bounded outputs, no credential printing, and explicit receipts. They do not provide generic shell-over-MCP or consumer product semantics. Read the script and its matching tests before use.
