# L5 rollout handoff — 2026-08-11

This is the Agent-readable execution handoff for connecting every registered
consumer to runtime-env L5. It records measured evidence and blockers; it does
not replace the normative procedure in `docs/l5-consumer-integration.md`.

## Authority and hard boundaries

- The user explicitly authorized the five external-content operations for AGY
  replay, DR research, repo-wiki authoring, Gemini conversation research, and
  bettor technical-equivalence research.
- The user did **not** authorize an iOS build or upload. Do not invoke either.
  Existing iOS verify and beta-readiness receipts are read-only evidence only.
- Never copy raw model output, conversation content, credentials, cookies, or
  dotenv values into the repository or Agent context. Keep content under the
  declared broker roots and report only bounded metadata.
- A failed workload is not L5. A receipt bound to an older runtime or consumer
  commit is not evidence for a newer pair of revisions.

## Revision boundary

The first rollout receipts below were produced by runtime commit
`9ebbee650b7dc5ccc82aab7e40f0b899050a2549`. Runtime commit `801890c` then
fixed consolidated acceptance so a declared `mutation=workspace` may change
only ignored/untracked evidence while HEAD, tree, index, and non-ignored paths
remain immutable. Therefore every consumer must be repinned and reaccepted at
`801890c` or a descendant before any all-current L5 claim.

## Measured rows

| Workload | Measured result | Evidence / required next change |
|---|---|---|
| `agy-gemini36-flash-high-replay` | Passed L5 at runtime `9ebbee` | All three fixed entrypoints passed. Receipt: `/Users/neon/.local/state/runtime-env/receipts/l5-20260811/skill-bettor-agy-current.json`. Repin and reaccept; do not retransmit merely to refresh a projection unless the fixed acceptance set requires it. |
| `dr-research-loop` | Live entrypoints passed, consolidated result below L5 at runtime `9ebbee` | Two acceptances changed ignored trajectory evidence. Receipts: `skill-bettor-dr-current.json` and `skill-bettor-dr-rerun.json` in the same private receipt root. Runtime `801890c` addresses this declared-workspace case. Repin, then make one final acceptance attempt; stop if it fails. |
| `repo-wiki-converge` | Failed after three author attempts; STOP external retries | A direct `refine` reached preverify PASS, but the fixed `author` adapter always called `create` and regressed the broker pages; the consolidated workload also lacks an independent judge execution plane. Local ix-agy commit `f6d3b2c` now plans create/refine/complete explicitly, refuses to overwrite an existing unjudged wiki, and requires mechanical plus `CONVERGED=true SCORE=100` evidence before ingest. It is not published or reaccepted. Last failed receipt: `ix-agy-repo-wiki-rerun.json`. |
| `gemini-conversation-research` | Live extension extraction passed; fixed consolidated L5 not reached; STOP external retries | The bounded extension adapter extracted one conversation to the broker without returning raw content to main context. Local ts-skill-bettor commit `4e2ae20` fixes guided input by removing embedded candidates when `--projection` is used and binding terminal receipt/path/hash/turn evidence. It is not published or rerun. The remaining blocker is architectural: the projected standalone adapter supports loopback CDP only, while the authenticated carrier available during measurement was the Chrome extension. |
| `bettor-arena-proof` | Failed; STOP live retries | Consumer commit `825784d` makes the tracked verifier executable. Offline proof then found admitted-mirror hard drift. The digest-bound live operation completed primary and gap-01 research but gap-02 lost its CDP page/frame and produced no candidate, so no sync bundle or Human-admit decision exists. Receipt: `bettor-equivalence-live-final.json`; broker adapter receipt names a failed third invocation. Do not retry without a tested page-recovery/resume fix. |
| `ios-testflight-verify` | Passed L5 at runtime `9ebbee` | Read-only receipt: `ix-agy-ios-verify-current.json`. Repin/reaccept may run only the declared read-only verify workload; no build/upload. |
| `ios-testflight-beta` | Passed L5 readiness at runtime `9ebbee` | Receipt: `ix-agy-ios-beta-current.json`. This is preflight evidence, not an uploaded build. No build/upload is authorized. |
| `stealth-browser-mcp` | Passed L5 at runtime `9ebbee` | Receipt: `stealth-browser-current.json`. Repin and reaccept on the current runtime revision. |

All relative receipt names above live under
`/Users/neon/.local/state/runtime-env/receipts/l5-20260811/` and must remain
user-owned private files.

## Local commits not yet published

- runtime-env `801890c`: honor declared workspace mutation while preserving
  tracked-state immutability.
- runtime-env `cd1e30e`: add this rollout handoff.
- bettor-arena `825784d`: make the tracked L5 verifier executable.
- ix-agy `f6d3b2c`: make repo-wiki author state-aware and fail closed on missing
  judge evidence.
- ts-skill-bettor `4e2ae20`: fix the guided projection/terminal-artifact input
  contract.

GitHub publication was not authorized by the five external-content-operation
grant. An attempted runtime branch push was rejected before any remote mutation.
Do not push, open PRs, or merge these commits until the user explicitly grants
GitHub publication authority.

## Required implementation order

1. Publish runtime commit `801890c`, install that immutable CLI, and repin each
   consumer projection without switching a shared main checkout.
2. Land bettor verifier mode commit `825784d` through review; do not call it L5
   merely because the hook gate now executes.
3. Reaccept already-green rows on the new revision, preserving the iOS
   no-build/no-upload boundary.
4. Make the repo-wiki state-aware author/judge change and the Gemini
   carrier/guided-input changes with failing public-seam tests first.
5. Make bettor's persistent-CDP adapter resumable after a detached/closed page,
   then obtain a fresh candidate bundle and request a separate Human admit for
   the exact sync bundle. External transmission authority is not sync authority.
6. Report completion only when every row has one consolidated L5 receipt bound
   to the current consumer and runtime commits. Until then, report each row
   independently and keep the overall rollout incomplete.
