# `profiles/`

Owner: composition of modules for one workload/runtime class.

```text
profile selected → modules resolved → defaults/scopes checked → portable closure
```

Profiles do not contain secret values and do not prove host/provider readiness. Local and cloud opt-in profiles remain distinct.
