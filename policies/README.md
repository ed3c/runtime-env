# `policies/`

Owner: carrier-native Claude/Codex/OpenShell isolation projections.

```text
policy selected → contract validated → consumer-native projection → host approval/canary
```

A policy declaration does not grant permission, establish authentication, or prove a live session. Secret placeholders remain opaque and broker-owned.
