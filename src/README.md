# `src/`

Owner: implementation packages. [`runtime_env/`](runtime_env/README.md) contains the CLI transition engine. Public behavior is defined by the root `runtime-env` launcher, schemas, tests, and documented exit semantics.
